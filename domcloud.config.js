module.exports = {
  buildCommand: 'npm run build',
  outputDirectory: '.next',
  installCommand: 'npm install',
  devCommand: 'npm run dev',
  framework: 'nextjs',
  nodeVersion: '18.x',
  env: {
    NODE_ENV: 'production'
  }
}
